package com.sensors.ocr_test.enums;

public enum SensorTypeEnum {
	PRESSURE,
	VOLTAGE,
	TEMPERATURE,
	HUMIDITY;

	// private SensorType sensorType;

	// private SensorTypeEnum(String type, String unit){
	// 	sensorType = new SensorType(type, unit);
	// }
}
